generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder — full schema generated in Phase 5 Batch 0
// All 18 entities defined in .specify/specs/001-haversack-unified-platform/data-model.md
